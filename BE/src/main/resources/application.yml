spring:
  application:
    name: BE

  datasource:

    url: jdbc:mysql://localhost:3306/lab_manager
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        
  # Email configuration
  mail:
    host: smtp.gmail.com
    port: 587
    username: codemaster3979@gmail.com

    password: bnzrqpzdxjfknmxk

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
        transport:
          protocol: smtp
        debug: false
  security:
    oauth2:
      client:
        registration:
          facebook:
            client-id: 2510919139289428
            client-secret: c0daca5fb8b83a9e1b2532fb971808fb
            redirect-uri: "${FACEBOOK_REDIRECT_URI:http://localhost:3000/user/auth/social/callback}"
            auth-uri: "https://www.facebook.com/v11.0/dialog/oauth"
            token-uri: "https://graph.facebook.com/v11.0/oauth/access_token"
            user-info-uri: "https://graph.facebook.com/me?fields=id,name,email,picture.type(large)"
            scope: "email%20public_profile"
            response-type: code

          google:
            client-id: "${GOOGLE_CLIENT_ID:xxxxxxxxx}"
            client-secret: "${GOOGLE_CLIENT_SECRET:xxxxxxxxxx}"
            redirect-uri: "${GOOGLE_REDIRECT_URI:http://localhost:3000/auth/social/callback/google}"
            auth-uri: "https://accounts.google.com/o/oauth2/v2/auth"
            token-uri: "https://www.googleapis.com/oauth2/v3/token"
            user-info-uri: "https://www.googleapis.com/oauth2/v3/userinfo"
            scope:
              - email
              - public_profile

jwt:
  secret: nXT5iZ/lPUY/9T4pgt3zqCJkezyP0if70mQbfJlz1k8=
  expiration: 86400
