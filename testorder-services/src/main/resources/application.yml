spring:
  application:
    name: testorder-service

  datasource:

    url: jdbc:mysql://localhost:3306/testorder

    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 8083

app:
  patient-service:
    url: http://localhost:8082

springdoc:
  swagger-ui.enabled: true
  api-docs.enabled: true